User prompt: Build fails at repo root because Angular workspace lives in packages/ui-angular; firebase deploy --only hosting also fails likely due to working dir/target. Need minimal changes so ng build works from root and firebase deploy hosting works.

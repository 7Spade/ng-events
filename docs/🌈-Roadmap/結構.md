packages/core-engine (Pure Domain Logic)
core-engine/
├── aggregates/
│   ├── base/
│   │   └── .gitkeep
│   └── __tests__/
│       └── .gitkeep
├── causality/
│   ├── metadata/
│   │   └── .gitkeep
│   └── __tests__/
│       └── .gitkeep
├── event-store/
│   ├── interfaces/
│   │   └── .gitkeep
│   ├── events/
│   │   └── .gitkeep
│   └── __tests__/
│       └── .gitkeep
└── projection/
    ├── interfaces/
    │   └── .gitkeep
    └── __tests__/
        └── .gitkeep
packages/saas-domain (Business Models)
saas-domain/
├── task/
│   ├── aggregates/
│   │   └── .gitkeep
│   ├── value-objects/
│   │   └── .gitkeep
│   ├── events/
│   │   └── .gitkeep
│   └── __tests__/
│       └── .gitkeep
├── payment/
│   ├── aggregates/
│   │   └── .gitkeep
│   ├── value-objects/
│   │   └── .gitkeep
│   ├── events/
│   │   └── .gitkeep
│   └── __tests__/
│       └── .gitkeep
└── issue/
    ├── aggregates/
    │   └── .gitkeep
    ├── value-objects/
    │   └── .gitkeep
    ├── events/
    │   └── .gitkeep
    └── __tests__/
        └── .gitkeep
packages/platform-adapters (SDK Implementations)
platform-adapters/
├── firebase/
│   ├── admin/        # Backend (firebase-admin)
│   │   ├── event-store/
│   │   │   └── .gitkeep
│   │   ├── projection/
│   │   │   └── .gitkeep
│   │   └── __tests__/
│   │       └── .gitkeep
│   └── angular-fire/ # Frontend (@angular/fire)
│       ├── auth/
│       │   └── .gitkeep
│       ├── query/
│       │   └── .gitkeep
│       └── __tests__/
│           └── .gitkeep
├── auth/
│   ├── admin/
│   │   └── .gitkeep
│   ├── client/
│   │   └── .gitkeep
│   └── __tests__/
│       └── .gitkeep
├── notification/
│   ├── fcm/
│   │   └── .gitkeep
│   ├── email/
│   │   └── .gitkeep
│   └── __tests__/
│       └── .gitkeep
├── analytics/
│   ├── google-analytics/
│   │   └── .gitkeep
│   └── __tests__/
│       └── .gitkeep
└── ai/
    ├── genai/
    │   └── .gitkeep
    ├── vertex/
    │   └── .gitkeep
    └── __tests__/
        └── .gitkeep
packages/ui-angular (Angular Frontend)
ui-angular/
├── angular.json      ← Moved from root
├── src/
│   ├── app/
│   │   ├── features/
│   │   │   ├── task/
│   │   │   │   ├── components/
│   │   │   │   │   └── .gitkeep
│   │   │   │   ├── containers/
│   │   │   │   │   └── .gitkeep
│   │   │   │   └── services/
│   │   │   │       └── .gitkeep
│   │   │   ├── payment/
│   │   │   │   ├── components/
│   │   │   │   │   └── .gitkeep
│   │   │   │   ├── containers/
│   │   │   │   │   └── .gitkeep
│   │   │   │   └── services/
│   │   │   │       └── .gitkeep
│   │   │   └── issue/
│   │   │       ├── components/
│   │   │       │   └── .gitkeep
│   │   │       ├── containers/
│   │   │       │   └── .gitkeep
│   │   │       └── services/
│   │   │           └── .gitkeep
│   │   └── shared/
│   │       ├── components/
│   │       │   └── .gitkeep
│   │       ├── services/
│   │       │   └── .gitkeep
│   │       └── utils/
│   │           └── .gitkeep
│   └── __tests__/
│       └── .gitkeep
└── e2e/
    └── __tests__/
        └── .gitkeep